# ============ Production runtime dependencies ============
Django>=5.1,<5.3
psycopg[binary]>=3.2
python-dotenv>=1.0
python-calamine>=0.2
openpyxl>=3.1                # .xlsx export (monthly report download)

# WSGI server + static file serving for production
gunicorn>=22.0
whitenoise>=6.7